<%= simple_form_for(@work_category) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :name %>
    <%= f.input :htmlId %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
