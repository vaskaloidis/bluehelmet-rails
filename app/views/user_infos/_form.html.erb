<%= simple_form_for(@user_info) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :name %>
    <%= f.input :value %>
    <%= f.association :User %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
